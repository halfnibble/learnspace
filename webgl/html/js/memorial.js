var MemorialDay = (function() {
	// Memorial Day Is Everyday
	var now = new Date(),
		forever = Infinity,
		silence = "";

	for (var i = now; i < forever; i++) {
		/* Remember Those
		 * Who Sacraficed
		 * Everything.
		 * Always.
		 */
		console.log(silence);
	}
}());

// NOTE: Do NOT run this script!!